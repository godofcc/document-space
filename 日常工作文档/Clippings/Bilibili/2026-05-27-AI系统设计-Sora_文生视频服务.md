---
title: "AI系统设计-Sora/文生视频服务"
url: "https://www.bilibili.com/video/BV1yDR8BwE3y/?spm_id_from=333.1387.upload.video_card.click&vd_source=bb9b0088c91e32867b62d41f2026ab57"
bvid: "BV1yDR8BwE3y"
cid: "38144576985"
author: "s09g"
upload_date: "2026-05-06"
subtitle_lang: "中文"
created: "2026-05-27"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116529005857838&bvid=BV1yDR8BwE3y&cid=38144576985&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

0:00 Design Text-to-Video System
1:41 功能与非功能需求梳理
5:27 Admission Control
7:00 预处理
8:20 模型推理
9:29 后处理
10:24 DAG 与状态机设计
15:48 故障恢复、降级与跨区域调度
18:07 视频交付：上传 CDN 与自适应下载

## 字幕

`00:00` 本期我们来进入 AI+System Co-design
`00:02` 我们这次来看 OpenAI 的一道面试题
`00:05` design text-to-video system
`00:07` 以前他们这题叫做 design Sora
`00:09` 但是本质上
`00:10` 是要求你设计一个
`00:11` 从文字生成视频的这么一个服务
`00:14` 首先用户他想要的业务是什么样的
`00:18` 用户肯定是希望他能够尽快的
`00:21` 拿到一段
`00:22` 生成好的视频
`00:23` 这些视频应该质量上有保证
`00:25` 说到视频的质量
`00:26` 主要是这么几个方面
`00:27` 第一个时间上的一致性
`00:29` 你的物体生成完之后
`00:31` 它不应该随机的去变形
`00:33` 或者是从视频上就突然消失掉
`00:35` 第二个它在物理上应该是合理的
`00:38` 光打下来它有阴影
`00:39` 镜子上它有反射
`00:41` 东西落地了
`00:42` 应该是能够沿着重力的方向
`00:44` 去产生变化
`00:46` 第三个应该是你的清晰度要高一点
`00:49` 至少得是 720p 吧
`00:50` 那现在手机基本上都是以这个为最低了
`00:54` 那除此之外呢
`00:55` 我觉得你还要保证另外这么几点
`00:57` 一个是它的 cost 应该比较合理
`01:00` 如果这东西太贵
`01:01` 那用户很可能不会去使用它
`01:03` 第二个它应该能支持一定量的吞吐
`01:07` 如果你一直在排队等待
`01:09` 那么
`01:09` 用户很可能也不会喜欢你们的服务
`01:12` 最后就是 safety 上的要求
`01:14` 对于服务提供商来说
`01:16` 你应该保证你的视频
`01:17` 不能用于一些比较危险的方向
`01:20` 比如说你的系统应该拒绝生成明星
`01:23` 或者是公众人物的肖像
`01:24` 或者是暴力内容
`01:26` 甚至有的时候
`01:27` 你可能会遇到一些法务上的问题
`01:29` 所以你应该保证
`01:30` 你不能生成一些特定的 IP
`01:33` 最后可能
`01:33` 要求你在视频的每一帧当中
`01:35` 都插入一个 watermark
`01:37` 标记你的视频是 AI 生成的
`01:39` 理清了这些业务需求之后
`01:41` 我们就可以来去梳理一下
`01:43` 它的功能性需求
`01:45` functional requirements 大概是这么几条
`01:47` 首先用户
`01:47` 他应该可以通过文本
`01:50` 提示词来生成视频
`01:51` 第二个视频的话
`01:53` 你应该至少是一个比较合理的长度
`01:55` 比如说 15 秒 720p
`01:58` 如果你是企业用户
`01:59` 我可能允许你拓展到 60 秒 1080p
`02:04` 但是我们先从 720p 开始
`02:05` 第三个
`02:06` 用户之后应该能接收到这个 video
`02:09` 这段可能是 async 的
`02:10` 因为中间生成时间我估摸着会比较长
`02:14` 所以用户可能会收到 video push notification
`02:18` 非功能性需求第一项你肯定可以想到
`02:20` 就是 scalability
`02:21` 你的服务至少要能撑起一定规模
`02:23` 比如说你可能一天 5 million 个 request 吧
`02:26` per day 的话
`02:26` 那相当于每秒钟平均下来只有 50 per
`02:29` second
`02:31` Peak QPS 可以估算是 200
`02:33` 所以相比于正常互联网服务
`02:34` 这也不是很高的一个数字
`02:36` 第二个是你生成视频
`02:38` 它会有一定的 latency 要求
`02:40` 这个其实还是比较 challenging 的
`02:41` 有两个 latency 指标
`02:43` 一个是你给用户直接感知
`02:44` 比如说 first frame
`02:45` 第一帧生成出来
`02:46` 你可能要多长时间
`02:47` 第二个你可能要计算的是
`02:50` 我整个视频生成完就是 end-to-end
`02:52` 我们先来关注这个 end-to-end latency
`02:54` 我估摸着你可能小于
`02:56` 先从5分钟开始吧
`02:57` 第三个应该是 efficiency
`03:00` 因为你这里用了很多 GPU 上
`03:01` 的资源
`03:02` 所以如果你 efficiency 不够高的话
`03:04` 你多少 GPU 都不够用
`03:05` 最后
`03:06` 应该是合规了
`03:07` 比如说没有公众人物
`03:10` 然后水印
`03:11` 你有了这些列举出来需求
`03:14` 我们可以大致是
`03:15` 先去画一个 high-level diagram
`03:25` high-level来看这里还是比较简单的
`03:27` 用户发起一个请求
`03:28` 首先会打中这个 API Gateway
`03:31` 那 Gateway 应该去查一下
`03:32` 你现在有没有足够的 quota
`03:34` 有没有这个余额
`03:35` 去进行这个视频的生成
`03:37` 如果有
`03:38` 那交给 backend server
`03:39` backend server 落盘这个数据之后
`03:41` 会把这个 request
`03:42` 转化成一个生成视频的 task
`03:45` 丢给这个 GPU worker
`03:47` 这边
`03:47` GPU worker 应该去调用这个 model
`03:49` 去生成这个视频
`03:51` 生成好视频丢到 CDN 上
`03:53` 然后给用户一个通知
`03:54` 用户可以去 CDN 上拉取他需要的视频
`03:57` 的清晰度
`03:58` OK，high-level
`03:59` 看起来还是比较简单的
`04:01` 但是这里有一个
`04:02` 我们平常不太会见到的东西
`04:03` 就是这里的 GPU
`04:05` GPU 是很贵的资源
`04:07` 你应该意识到是
`04:08` 你的 GPU 肯定不是无限的
`04:10` 那当你的资源受限的时候
`04:12` 有件事你一定要明白
`04:13` 你不是每个 request 都会打到这个 GPU 上
`04:17` 不然的话你的系统根本承受不住
`04:18` 你的成本也会很高
`04:20` 所以这里你要做的第一件事
`04:21` 一定是 admission control
`04:23` 就要去做一个准入控制
`04:25` 这里做准入控制
`04:26` 我们可以做一个两阶段
`04:27` 甚至三阶段
`04:29` 第一阶段
`04:29` 你决定这个 request
`04:31` 是否可以进入你的系统
`04:32` 第二阶段你才决定是否将这个
`04:35` request 放入到昂贵的 GPU 执行阶段
`04:38` 所以这里第一阶段
`04:39` 你先去做 rate limiter
`04:41` 在这里去做一个限流
`04:43` 最简单就是查看用户的 quota
`04:45` 比如说他的 subscription
`04:47` 那免费的用户
`04:48` 他应该有的 quota 肯定是低一些
`04:50` 付费用户根据他不同价位的 plan
`04:52` 应该对应着不同的 quota 级别
`04:54` 用户
`04:55` request 进来
`04:55` 你可以先去这个 rate limiter 上查一下
`04:57` 如果有足够的余额
`04:59` 那就进入下一层啊
`05:00` 如果没有的话
`05:01` 你可以做一个 upsell
`05:02` 在这里弹个窗向用户提示
`05:04` 你可以去升级
`05:05` 给用户做一个销售
`05:07` 第二层就应该是 resource check 了
`05:09` 你要去看看你的线上有没有 GPU
`05:12` 直觉上 GPU 这种资源你就算有
`05:14` 那也是多个 team 之间必须要分配的
`05:18` 这里要看你 text-to-video service
`05:20` 它的优先级有多高
`05:21` 如果 product team 用得太多
`05:23` 那别的 team，比如说 research 那边
`05:25` 他的资源就不够了
`05:26` 不过 research 这边做 prototyping
`05:28` 通常它不会有很高的 QPS
`05:31` 所以说有的时候老板想多挣钱
`05:32` 就会把这个资源划到 production 那边
`05:35` 另外有一种可能是客户
`05:36` 可能 bring your own machine
`05:38` 或者是 bring your own capacity
`05:40` 他们会有自己的 GPU 集群
`05:42` 不去占用你的资源
`05:43` 但是希望你把你的模型和 service
`05:46` 部署到他们的 GPU 上去
`05:48` 这些都是你做 resource check 的时候
`05:49` 需要注意的
`05:50` 第三层才是最后的 traffic shaping
`05:53` 或者是 request shaping
`05:54` 这个就是流量整形
`05:56` 在这里你可以添加一个 queue
`05:58` 然后给不同的用户不同的优先级
`06:01` 比如说 free
`06:02` paid、enterprise 这种不同的用户等级
`06:05` 这里如果流量压力比较大呢
`06:07` 你可以去做一个服务降级
`06:09` 优先去保证付费用户
`06:10` 和企业用户的资源
`06:16` 还有一种潜在可能是
`06:17` 你可以去别的区域调集资源
`06:19` 一般来说
`06:20` 这种 long-running task
`06:21` 它对延迟其实没有这么敏感
`06:23` 你这里保证 latency
`06:25` 只要5分钟之内也就可以了
`06:26` 所以一个可行方案是
`06:28` 你可以利用一下时差
`06:29` 比如说北美这边资源不够
`06:31` 你可以把这个 request 调到欧洲去
`06:33` 那边可能是夜间
`06:35` 等这个任务在那边生成好了视频之后
`06:37` 再从欧洲传回来
`06:38` request 正式进入这个系统之后
`06:40` 你会在 backend server 上
`06:42` 应该生成一个对应的 workflow
`06:44` 那么接下来这个问题就是
`06:46` 你的 workflow 当中应该包含哪些步骤
`06:49` 你怎么划分
`06:50` 这个 workflow
`06:50` 其实会决定面试官对你的看法
`06:53` 你懂不懂 AI
`06:53` 其实这里一眼就能看出来
`06:55` 我们先从一个最简单的场景入手
`06:57` 假设这是 happy path
`06:58` 一个完整的 workflow
`07:00` 你会有哪些环节
`07:01` 第一步肯定是要去做一些预处理
`07:04` preprocessing
`07:05` 这一步主要是去准备你的输入
`07:08` 比如说
`07:08` 你要对用户给的 input text 进行一下 check
`07:13` 主要是你要避免用户的关键词当中
`07:15` 包含的比如说公众人物
`07:16` 或者是有一些暴力色情的要求
`07:19` 另外还有一点
`07:20` 就是会不会带有一些明显的版权内容
`07:24` 比如说迪士尼的 IP
`07:26` 这些都是不可以生成的
`07:27` 第二步
`07:28` 应该是 text
`07:28` rewrite 或者是 prompt enhancement
`07:31` 有的用户的输入
`07:32` 可能就只有几个关键词
`07:34` 但是如果你用过 Stable
`07:36` Diffusion 这类 model 的话
`07:37` 一个很明显的感受是
`07:38` 你给的这个 prompt 越详细越丰富
`07:41` 最后生成的效果就会越好
`07:43` 所以这里你需要做一步 text enhancement
`07:46` 把这些东西进行一下改写
`07:48` 如果用户只是改一些关键词的话
`07:50` 你需要把它变成一些完整的句子
`07:52` 那最后就是对你这个改写之后的文本
`07:56` 再去做一步 check
`07:57` 这一步其实也是挺重要的
`07:58` 因为 prompt enhancement
`08:00` 它其实不是负责 safety 的组件
`08:02` 它只是提升质量
`08:03` 它不负责判定我最后的这些提示词
`08:06` 是否是合理的
`08:08` 所以这里你需要去检查
`08:09` 我在 rewrite 之后
`08:10` 是否偏离了原有的意图
`08:13` 是否引入了一些不该有的细节
`08:15` 我最后送给模型的这个 prompt
`08:17` 是否仍然是合规的
`08:19` 做好了 preprocessing 之后
`08:21` 你会进入 model inference 阶段
`08:22` 就是模型的推理
`08:24` 那这里的推理
`08:25` 大致上会分也是这么三步法
`08:27` 第一步
`08:28` 你的 text 应该是变成一个 embedding
`08:31` 这里你需要 text encoder
`08:33` 在这里
`08:34` 呃 text encoder
`08:35` 可能是一个到三个这样的模型
`08:37` 然后它们也会跑在你的 GPU 上
`08:40` 同时除了 text 之外呢
`08:42` 你应该还要给上一个 random noise
`08:45` 作为最初的模型生成的种子
`08:49` 第二阶段
`08:50` 就是我们说的 Diffusion Transformer 了
`08:52` 这个是用来生成视频的那个模型
`08:54` 一般来说
`08:54` 这种都是 dense model
`08:56` 如果你看过前两年 Meta 那篇论文的话
`08:59` 这个东西一般是 30B
`09:01` 左右的参数
`09:02` 然后分布在 8 个 GPU 上
`09:04` 这个模型
`09:05` 它会使用我们之前给到的 latent
`09:08` space 的输入
`09:09` 然后生成一个 denoised 之后的 latent
`09:12` 有了这个 latent
`09:13` 你需要再经过一步视频层面的 decoder
`09:16` 比如说使用 TAE 或者 VAE 这样的东西
`09:19` 将这个 latent 转化成正常的 video
`09:21` 这几步其实都是跑在 GPU 上的
`09:23` 当你有了 video 之后
`09:25` 你还是不能直接丢给用户
`09:27` 你需要一步 post-processing
`09:29` 这里的话
`09:30` 需要看你的工作流是什么样子了
`09:33` 可能第一步是一个 video enhancement
`09:35` 默认你生成的视频应该是 720p
`09:38` 乘上 15 秒到 16 秒
`09:40` 但是如果是企业用户
`09:41` 我们可能允许他去生成 1080p
`09:43` 这个级别的 video
`09:45` 那么这里你需要做一些 super resolution
`09:48` 比如说
`09:48` 我使用一个 spatial super resolution 的 model （SSR）
`09:51` 或者是做一个插帧
`09:52` 在时间上做一个 super resolution
`09:54` 这些都是有可能的
`09:56` 第二步是对你的生成的视频
`09:57` 去做一个 safety check
`09:59` 那这可能是你的最后一道防线了
`10:02` 如果在这里你生成的视频
`10:03` 你发现它
`10:04` 可能不小心
`10:05` 引入了一些不适合出现的东西
`10:08` 这里就是你最后一次
`10:09` 把它拦下来的机会
`10:10` 最后一步应该是加水印了
`10:12` 你需要加一个 watermark
`10:14` 来标记你这个视频是由 AI 生成的
`10:17` 这些都搞定之后
`10:18` 才可以去 deliver 到这个 CDN 上
`10:20` 然后通知用户去把这个视频下载下来
`10:22` 之前我们也说过这类 async long-running
`10:24` task 通常会用到状态机
`10:27` 而对于这种多阶段的任务
`10:28` 你需要引入一个 DAG
`10:30` 在这里
`10:30` 我们把每一个 workflow 展开成多个 stage
`10:34` 每个 stage 里面还有自己的 subtask
`10:36` 每个 subtask 会有自己的状态机
`10:40` 我稍微把这个图改一下
`10:47` 哦，这里有一个 DAG
`10:49` 首先是 admission control
`10:52` 然后是 preprocessing
`10:54` 接下来是 model 的 inference
`10:57` 然后是 post-processing
`10:59` 最后才是 deliver
`11:01` OK
`11:03` 这是你的 DAG 展开
`11:05` 然后是你的状态机
`11:08` 每一个任务都可以从 ready 开始
`11:11` 然后一旦这个任务进入调度
`11:13` 我们可能会让它进入到一个 scheduling
`11:16` 阶段这里我给它改个名
`11:18` 这个叫做 workflow controller
`11:20` 它会去管理我的调度
`11:22` happy path 的话
`11:23` 它是进入 running
`11:24` 最后是 complete
`11:26` 但是不一定所有都是 happy path
`11:28` 所以它有可能从 running 转入到
`11:31` 这里是一个 retriable event
`11:33` 所以你可能进入 retry
`11:34` 那 retry 就相当于我还要回到这个 ready
`11:36` 再重新进入 scheduling
`11:39` 那么另一种可能是
`11:40` 我 running retry 多次之后
`11:43` 它还是失败了
`11:44` 那这里就是一个 failure path
`11:46` OK
`11:47` 有了这个状态机和这个 DAG
`11:49` 很多问题会变得清晰起来
`11:52` 第一个问题
`11:53` 我怎么去做这个调度
`11:55` 这里你可以看到不同的阶段
`11:57` 它的任务目标和执行的环境是不同的
`11:59` preprocessing 这里主要是处理这个输入
`12:02` 你需要去做 safety
`12:03` text check，去做这个 text rewrite
`12:06` 你的输入输出主要是文本
`12:07` 所以它的开销会比较低
`12:09` 延迟也比较低
`12:10` 计算上会比较便宜
`12:12` 你可以使用一些 CPU
`12:13` 或者是一些小的 GPU 去完成
`12:15` 延迟可能也就是几百毫秒的样子
`12:18` 第二阶段在这里
`12:19` model inference
`12:20` 这里就主要是这个 Diffusion
`12:23` Transformer 和 TAE (or VAE) decoder
`12:25` 甚至还可能考虑到这个 text encoder
`12:27` 这些东西需要占据你的显存
`12:29` 需要使用你的 GPU 的计算
`12:31` 一般来说
`12:32` 这里会比较贵
`12:33` 你可能需要 8 个 GPU 并行去执行
`12:35` 那这里
`12:36` 我们会使用一个 gang scheduling 的方案
`12:39` 8 个 GPU 需要同时占用或者同时释放
`12:41` 第三阶段
`12:42` 这个 post-processing 主要是提升质量
`12:44` 这里的话
`12:45` 有可能你使用一个单 GPU
`12:47` 就可以搞定了
`12:48` 而且 video enhancement
`12:50` 这部分不是所有人都会用到
`12:52` 如果你是免费用户
`12:54` 或者是你付费的 plan 比较低的话
`12:56` 你可能通常不会有这一步
`12:58` 所以这是一个 optional 的
`13:00` 这边你可能不需要用最贵的那种 H100
`13:03` 你可以使用一些比较旧的
`13:05` 或者比较小的 GPU 去完成
`13:07` 这样可以节省一定的成本
`13:09` 最后就是 deliver 阶段
`13:10` deliver 阶段甚至于你就是用不到 GPU 了
`13:13` 通常也就是一些 CPU 上的 task 就完成了
`13:15` 所以第一阶段你需要使用一个 GPU
`13:20` 第二阶段应该是 8 个 GPU
`13:23` 第三阶段又是一个 GPU
`13:24` 除了调度层面的差异
`13:26` 我们还可以推断出另一东西
`13:28` 就是这个任务它能否暂停
`13:30` 或者说支不支持我断点重续
`13:33` 这里假设你的任务想要暂停的话
`13:35` 你需要存中间的 checkpoint
`13:37` 至少需要存三个东西
`13:39` 第一个你的任务的进度
`13:41` 比如
`13:41` 说我当前到了哪个 stage
`13:43` 这个就是你的 DAG 上的信息
`13:46` 第二个我的中间产物
`13:48` 中间产物会有很多种不同的东西
`13:51` 预处理阶段的产物
`13:52` 它是一些 text
`13:54` 这个东西比较好存
`13:55` 但是到了模型推理阶段
`13:57` 它的中间产物就不一样了
`13:59` 一旦进入了 encoding
`14:01` 你应该会拿到一个 text embedding
`14:03` 和一个用于去噪的这个 latent
`14:06` 你要考虑到
`14:07` 它应该是一个 spacetime patchify 的样子
`14:11` 比如说如果你去看MovieGen的那篇论文
`14:13` 它应该是 32*4*96*96 这么一个 Tensor
`14:18` 如果你使用 FP16 去存的话
`14:22` 这里大概是 0.7 个兆
`14:24` 也不是很大
`14:25` 问题是
`14:25` 接下来你要将这个 latent
`14:28` 把它转化成 video
`14:29` 这一步的话
`14:29` 你就需要和前面的 encoder
`14:32` 做个反向的 decoding 计算了
`14:34` 最后应该生成的是 720p 乘以 256 个 frames
`14:38` 也就是长宽都是 720
`14:40` 720 乘 720 乘 256
`14:42` 这里还有 RGB 三个 channel
`14:44` 所以是乘三
`14:47` 这里如果你用 uint8 来存的话
`14:49` 大概会用到 500 兆左右了
`14:51` 那如果你接下来再进一步
`14:54` video enhancement 呢
`14:56` 比如说
`14:56` 我通过 SSR 去把它提升到 1080p 的画质
`15:01` 很可能你大概要翻上4倍
`15:04` 所以 500 兆左右会变成 2GB
`15:06` 甚至于接下来你要进行插帧
`15:08` 将 256 个 frame 拓宽到 320 个 frame 吧
`15:12` 这个应该超出我的估算了
`15:13` 这可能估摸就是 2.4GB 了
`15:16` 但是到 video encoding 的时候
`15:17` 你又会根据你的码率去将它压缩下来
`15:21` 一般来说15秒的视频
`15:23` 你可能不会超过50兆
`15:24` 但是如果是一分钟
`15:25` 还是有可能上到180兆的样子
`15:29` OK，所以中间产物还是比较大
`15:31` 尤其是在这个位置上
`15:33` 不过中间产物只是一部分
`15:34` 你还要存第三个东西
`15:36` 就是我中间的激活状态
`15:39` Diffusion Transformer
`15:39` 它中间状态是 30B 的参数
`15:43` 那代表着你需要用 60GB 的显存
`15:45` 好了结论你可以得出来了
`15:48` 一旦进入到 model inference
`15:49` 就不要再中断它了
`15:51` 保存中间的激活状态
`15:52` 这个开销太大了
`15:54` 而你整个任务其实也就 30 秒
`15:56` 不太值得
`15:58` 第二个
`15:58` 30秒等待时间其实也不算太长
`16:01` 你可以加一个 draining 阶段去进行排水
`16:03` 你让现有的已经在跑的任务
`16:05` 尽量地去 deliver
`16:07` 第三个我们就不要提供这个 preemption
`16:11` 这种抢占操作
`16:12` 抢占你可以发生在 GPU 计算开始之前
`16:14` 也就是交给这个 admission control
`16:16` 这边去处理
`16:17` 你可以进行服务降级
`16:18` 或者是在 queue 里面排一个 priority
`16:21` 你需要在一开始
`16:22` 就给你的企业用户
`16:23` 和你的 VIP 用户留好足够的 quota
`16:26` 但是还有一个问题
`16:28` 比如说你 GPU 的 quota 就是不足
`16:29` 怎么办
`16:30` 比如说你不是主动想去抢占这个任务
`16:33` 而是说我当中有一部分 GPU 宕机了
`16:37` 导致我不得不进行服务降级
`16:40` 服务降级
`16:40` 其实
`16:41` 还是有一些比较标准的解决方案的
`16:44` 第一个
`16:45` 单 GPU 故障还比较简单
`16:46` 你直接开一个新的 GPU instance
`16:50` 然后去上面进行 retry
`16:51` 第二个如果我不是单 GPU 故障
`16:53` 我是整个 GPU instance 故障
`16:56` 那这里就说明我一次性宕机了 8 个 GPU
`16:59` 那这样还是同样
`17:02` 你还是去 retry 一个新的 GPU instance
`17:04` 但同时
`17:05` 你的 load balancer 上要去做一个 failover
`17:08` 将后端的这个 failed instance 给它
`17:11` 从 load balancer pool 里面去掉
`17:13` 第三个如果我的 traffic
`17:16` 还是持续的大于我现在已有的 quota
`17:19` 最简单的方案
`17:20` 我先延长你们等待时间
`17:22` 让所有的请求变慢
`17:24` 看看能不能通过这个 queue
`17:26` 把这些 traffic 先 buffer 住
`17:28` 然后再来慢慢消化
`17:29` 但如果我的 queue 还在不停地增长呢
`17:31` 这种情况下
`17:32` 你只能让免费的用户先等一等了
`17:34` 优先保证你的 VIP 用户和企业用户
`17:38` 那如果说我的故障是整个区域级别的
`17:41` 我就是某个数据中心全都宕机了
`17:44` 这种情况下
`17:45` 只能调度到别的 data center 了
`17:47` 最后的情况就是
`17:47` 我使用了上面所有的手段
`17:49` 但是我现在的容量还是持续的不足
`17:52` 这种情况下也没有别的好方法
`17:54` 你只有去召回你那些离线的 GPU
`17:57` 比如说他可能还在跑一些离线任务
`17:59` 正在做训练做实验
`18:00` 或者是做一些批处理
`18:02` 这种情况下
`18:03` 只能先保证 production 的供应量
`18:06` 最后你的视频生成好之后
`18:07` 应该上传到 edge CDN
`18:10` 传到离客户比较近的地方
`18:11` 给客户发一个通知
`18:13` 客户收到这个通知之后
`18:14` 可以根据他自己的网络状况
`18:17` 去做一个 adaptive downloading
`18:19` 去浏览
`18:20` 或者去下载不同的清晰度的视频
`18:22` 到这里你应该完成了整个 design
`18:25` 如果你还有时间的话
`18:27` 那就是你的 show-off time
`18:29` 一般来说
`18:29` 这种情况下
`18:30` 我推荐你和面试官去讲一讲优化
`18:33` 比如说最简单的
`18:35` 怎么去做 GPU 的并行
`18:37` 我们能不能使用一个单 GPU 跑
`18:39` 我这里计算的瓶颈在哪里
`18:41` 首先是计算量
`18:42` 一个视频 256 帧，16 秒
`18:45` 乘以 16 frames per second
`18:47` 这是多少
`18:48` 720p 的 pixel
`18:50` 768 乘以 768
`18:52` 然后乘以前面的 256 个帧
`18:55` 那经过 TAE (or VAE) 的压缩的话
`18:58` TAE (or VAE) 压缩是 8*8*8
`18:59` 会变成96*96*32
`19:02` 再经过一步的 patchify
`19:05` patchify 是一个 2*2*1 的 window
`19:07` 最后会得到 48*48*32 的这么一个序列
`19:11` 也就是说
`19:12` 我一个 video 压缩完之后
`19:15` 至少是 73K 的 sequence length
`19:17` 这个计算量其实在大多数情况下
`19:19` 是比 LLM 要大的
`19:21` language model
`19:23` 通常是自回归的
`19:24` 你每次向前只生成一个 token
`19:26` 在这个过程中
`19:27` 你还可以利用 KV cache
`19:29` language model 的计算量是多少
`19:30` 这个量应该是24
`19:33` n*d^2
`19:35` 乘以 L 层
`19:38` 再加上 2 n平方 乘以 d 乘以 L 层
`19:44` 这里的 d 是你的 hidden dimension
`19:46` n 是你的 sequence length
`19:48` 假设我们就去看 Llama 3
`19:49` Llama 3 是一个 dense model
`19:51` 但其实还是比较好算
`19:52` 你直接用它的
`19:54` parameter 就可以粗略去算一下
`19:56` 比如说 70B 的 Llama
`19:57` 那就是 2*70
`20:00` 乘以比如说 sequence length 是 1,000
`20:02` 其实你一句话应该也挺够的
`20:04` 那这里其实只有大概 140T FLOPs
`20:08` OK，这么多
`20:10` 但是如果是 Diffusion Transformer 呢
`20:14` Diffusion Transformer 的计算它是双向的
`20:17` 你每次会生成所有的
`20:18` token，并且你需要经过 50 个 denoising steps
`20:22` 每个 step 当中
`20:23` 你所有的 token
`20:24` 都会相互去算这个 attention
`20:26` 所以计算量是 n square
`20:29` Diffusion Transformer total FLOPs
`20:32` 这怎么估算？
`20:33` 这个应该是你的线性层
`20:34` 加上你的 attention 层
`20:35` 再去乘你的 denoising steps
`20:38` 这个如果你推一下的话
`20:39` 它也是24
`20:40` n * d^2 乘以这个 L
`20:43` 就是我乘以我的 layer 的数量
`20:45` 但是这里我还要再乘一个 denoising steps
`20:48` 所以我多了一个 s 在这
`20:49` 24 很明显是 FFN 来的
`20:51` 那你还要接下去算
`20:53` self-attention 和 cross-attention
`20:55` 这里如果你自己知道的话
`20:57` 你可以直接抛出来
`20:58` 4 倍的 n square 乘以 d、L、s
`21:01` 那 d 是我的 hidden dimension
`21:02` L 是我的 layer
`21:03` s 是我的 denoising steps
`21:04` 简便方法其实也有
`21:06` 你可以利用和之前相同的
`21:08` 我直接用参数去估算
`21:09` 2 乘以
`21:10` 30B 乘以
`21:12` 哎，sequence length 这里出了个问题
`21:14` 因为 sequence length 之前你算过
`21:15` 你知道它非常长 73K
`21:16` 降噪这里要经过 50 步
`21:18` 你还要乘上 50
`21:20` 加起来 220P FLOPs
`21:22` OK，所以这里直接就差几个数量级了
`21:25` 前面是 140T FLOPs
`21:27` 而且这还是一个下限的估算
`21:28` 我估计实际值的话应该还会更大一点
`21:32` 有了计算量
`21:33` 你大致可以去推算一下这个 latency
`21:35` 这里很明显啊
`21:36` 我的 self-attention 的这个 quadratic cost
`21:40` 是我的核心的瓶颈
`21:42` 那计算量这么大
`21:43` 延迟这么高
`21:44` 我怎么去降低它
`21:45` 最简单的一个方案就是
`21:47` 我去做多 GPU 并行
`21:48` 比如说你
`21:49` 可能听说过 USP
`21:51` 叫做 unified sequence parallelism
`21:53` 你的 self-attention 和 cross-attention
`21:55` 加上这个 FFN
`21:57` 一共有三次 reduce
`21:59` 那你可以把它合并成一个
`22:00` unified ring communication
`22:02` 不过生产上我们一般有 8 个 GPU
`22:05` 可能就直接去做 CP4 加 TP2
`22:08` 这里你大致去算一下模型的权重
`22:11` 这里 DiT 是 30B
`22:13` 这是 60G 除以 2
`22:14` 你拿到一个 30G
`22:17` text encoder 大概是 40GB
`22:19` 分在 8 张卡上
`22:20` 每个上面是 5GB
`22:21` 这里是 60GB
`22:22` 在两张卡
`22:24` 这里是 60GB
`22:25` 分成两个 tensor parallel pair
`22:26` TAE decoder
`22:28` 你可以使用一个蒸馏后的小模型
`22:31` 就是 14GB，然后分 8 张卡
`22:33` 那大概就是 2GB 左右
`22:36` 接下来是你的激活层
`22:38` 激活层主要来自于你的 Transformer
`22:40` 和你的 TAE (or VAE)
`22:41` 然后你还要考虑 all-gather 的 KV
`22:43` 也需要占用大概 20G 的空间
`22:46` 所以总共算下来
`22:47` H100 的 80G 也没有给你太多的冗余
`22:49` 另一种可能的并行方式就是 CFG 了
`22:53` CFG parallelism 好像也有人提过
`22:56` 因为你 CFG 需要同时运行
`22:57` conditional 和 unconditional
`22:58` 两次 forward
`22:59` 你可以在这里做一个并行优化
`23:01` 不过我一直觉得你做这个东西
`23:03` 你就不如去做这个蒸馏了
`23:05` 你可以使用一个蒸馏后的小模型嘛
`23:07` 比如说 Diffusion Transformer
`23:09` 在生产上我们通常都是用蒸馏模型的
`23:12` 然后 TAE (or VAE) decoder 也可以使用一个 lite 模型
`23:15` 第四种就是
`23:16` 你可以减少你允许生成的 sequence length
`23:18` 当然它的代价就是
`23:19` 你减少你生成视频的分辨率
`23:20` 和它的时长
`23:21` 别的还有一些
`23:22` 比如说可以去做 FP8 量化
`23:24` 或者是 Cache DiT
`23:26` 那这些东西
`23:26` 有条件你就自己看情况用吧
`23:28` 你觉得你熟哪些技术
`23:29` 你和面试官聊哪些