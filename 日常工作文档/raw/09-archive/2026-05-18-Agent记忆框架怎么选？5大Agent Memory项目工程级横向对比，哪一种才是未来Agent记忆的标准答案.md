---
title: "Agent记忆框架怎么选？5大Agent Memory项目工程级横向对比，哪一种才是未来Agent记忆的标准答案"
url: "https://www.bilibili.com/video/BV1p2DyB4Ee3/?spm_id_from=333.1391.0.0&vd_source=bb9b0088c91e32867b62d41f2026ab57"
bvid: "BV1p2DyB4Ee3"
cid: "37426170010"
author: "唐国梁Tommy"
upload_date: "2026-04-11"
subtitle_lang: "中文"
created: "2026-05-18"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116386449853382&bvid=BV1p2DyB4Ee3&cid=37426170010&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

Agent 为什么总"失忆"？本期系统拆解 5 个最具代表性的开源记忆框架，适合有 Agent开发基础、想搞清楚记忆层该怎么设计的同学，看完可以建立完整的认知框架。                       
                                  
 本期内容：                                                     
 - Text2Mem —— 给所有记忆系统定义"操作指令集"，12 个原子操作 + 五元 JSON 契约 + 双层验证
 - Mem0 —— 当下 Star 最多的记忆中间件，5 个工厂 / 双存储 / 三种记忆类型，含真实成本瓶颈分析             
 - Letta —— 把 OS 虚拟内存思想搬进 Agent，Git 版本化记忆 + Sleeptime 异步后台学习      
 - ReMe —— 阿里 AgentScope 出品，"文件即记忆"，记忆对用户完全透明可直接编辑                     
 - memU —— 范式最激进：让记忆本身变成一个 24/7 后台主动 Agent                                                               
                                                           
下集将继续聊 MemOS / OpenViking / Hindsight / Second Me / MetaMem —— 更偏系统级和前沿创新，记得点关注不迷路。 

更多前沿AI课程与学习资源请关注：
🌐 官方网站：TGLTommy.com(访问需科学上网)
📺 B站/公众号/YouTube：唐国梁Tommy

## 字幕

`00:00` 大家好
`00:00` 今天我们开一个新系列
`00:02` 专门聊一个特别重要
`00:03` 但很多人一直没搞透的话题
`00:06` agent的记忆
`00:07` 我相信你一定碰到过这种情况
`00:09` 昨天刚和AI聊完半小时的项目背景
`00:12` 今天打开新对话
`00:13` 他又把你当陌生人
`00:15` 一切从头再来
`00:16` 这不是bug
`00:17` 这是今天大多数LLM agent的默认形态
`00:21` 无状态
`00:21` 而要让agent真正长期为你工作
`00:24` 记忆就是绕不开的核心模块
`00:27` 本集讲这五个text to ma mem
`00:30` Zero later
`00:32` Remy mem u
`00:34` 它们代表了当前记忆框架的主流范式
`00:37` 这一集的目标是让你听完之后
`00:40` 对agent的记忆到底该怎么设计这件事
`00:43` 建立一个完整的认知框架
`00:45` 好我们开始我想先从一个可能很多人没听过
`00:49` 但我觉得特别有意思的项目讲起
`00:52` 它叫text to ma
`00:53` 为什么第一个讲他因为他做的事情非常基础
`00:56` 也非常野心大
`00:58` 他不是在做一个记忆系统
`01:00` 而是在给所有记忆系统
`01:02` 定义一套通用的操作语言
`01:04` 你想想现在这个场景
`01:05` 你对agent说一句
`01:07` 把我上周提到的那个会议纪要标记成重要的
`01:10` 30天后自动归档
`01:12` 这句话对人来说很清楚
`01:13` 但对于一个记忆系统来说
`01:15` 问题就来了
`01:16` 上周是哪一段时间
`01:18` 那个会议纪要对应数据库里的哪一条标记
`01:21` 成重要的
`01:22` 到底是改权重
`01:23` 改标签还是加提醒
`01:25` text to map的解法非常干脆
`01:27` 它在LLM和存储之间插一层L2
`01:30` 也就是中间表示层
`01:32` 你可以把它类比成CPU的指令集架构
`01:35` Text to main
`01:36` 把所有记忆操作收敛到12个
`01:38` 原指操作分成三个阶段
`01:40` ENC阶段只有encode负责记忆的诞生
`01:43` RET阶段两个操作
`01:45` retrieve是纯数据取回
`01:47` summarize是让LLM再生成一次摘要
`01:50` STO阶段九个操作覆盖即整个生命周期
`01:54` 我自己最欣赏的一个细节是
`01:57` 他把retrieve和summarize分开了
`01:59` 因为工程上他们完全不是一回事
`02:01` retrieve几10ms就返回
`02:03` 而summarize背后是一次完整的LOLM推理
`02:07` 延迟是上千倍
`02:08` 分成两个操作
`02:10` 执行引擎就可以用完全不同的超时缓存
`02:13` 降级策略
`02:14` Text to man
`02:15` 把所有操作都收敛成一段
`02:17` Jason
`02:18` 它是一个五元结构
`02:19` 第一个是阶段
`02:21` 第二个是操作名
`02:22` 第三个是目标
`02:23` 第四个是参数
`02:24` 第五个是原数据
`02:26` 这五个字段合在一起
`02:27` 就像一张记忆操作的标准公文
`02:30` 其中最能体现工程成熟度的是
`02:33` 原数据里藏的两个安全字段
`02:35` DREWRONG和confirmation
`02:37` 你想一种极端场景
`02:39` LLM抽风给你生成一条
`02:41` JASON说
`02:42` 把所有记忆全部删除
`02:44` text man在这里卡了一道硬性规则
`02:46` 你要么先用drive run模拟一次
`02:49` 要么显示的把confirmation设成true
`02:51` 二选一
`02:52` 少一个都不让过
`02:54` 光有安全阀还不够
`02:55` text man还上了两层验证
`02:58` 外层查的是格式和结构
`03:00` 走jason schema标准
`03:02` 里层查的是业务逻辑
`03:04` 靠PDENTIC做拦截
`03:06` 比如promote操作的绝对值和相对值
`03:09` 必须恰好有一个为空
`03:11` 这种不信任LLM
`03:12` 但能兜住LLM的设计哲学
`03:15` 才是text man真正值得学的地方
`03:18` text ma最有意思的设计点是它的lock操作四种模式
`03:22` read only完全冻结
`03:24` NO delete进删append only只允许追加custom
`03:28` 完全可编程
`03:30` 最喜欢的是review机制
`03:31` 本质上是RBAC风格的索绕过
`03:34` 当然text man目前还有明显短板
`03:37` 存储只有SQLIT参考实现
`03:40` 没有HTTPAPI向量
`03:42` 以JSON文本存在text列里
`03:44` 但它真正的价值是他在帮整个行业定义记忆
`03:48` 操作语言长什么样
`03:50` 讲完了地基
`03:51` 我们进入更偏实战的部分
`03:53` 第二个要讲的是
`03:54` 目前开源社区里热度最高的
`03:56` AIGE开源框架之1mam zero
`03:59` 它是VC孵化的项目
`04:00` GITHUB上的star数已经是几万级别
`04:03` 为什么这么多人选mam zero
`04:05` 原因很直接
`04:06` 上下文窗口有限
`04:08` 长对话里早期用户偏好会丢
`04:10` 每次新绘画都要重新介绍自己
`04:13` 不同绘画之间没有继承关系
`04:15` meme zero官方给出的数据是
`04:17` 相比直接用OpenAI memory
`04:19` 准确率高26%
`04:21` 响应快91%
`04:22` token省90%
`04:24` mem zero整体是三层
`04:26` 最上面是memory API
`04:28` 中间是LLM推理加向量检索的逻辑层
`04:32` 下面是存储层
`04:33` 真正能体现工程成熟度的是
`04:36` 中间的五个工厂模式
`04:38` l i m factory支持17加个LIM提供商
`04:41` embetter factory支持11加个embedding模型
`04:45` vector store factory支持22加种向量存储
`04:48` grab store factory支持四种图存储
`04:51` reentor factory支持五种REANCHOR
`04:54` 更妙的是
`04:55` 他用import lip做动态加载
`04:57` 你没装潘控包也没关系
`04:59` 只要用的不是潘控
`05:00` 就不会挂
`05:01` main zero在代码里显示定义了三种记忆类型
`05:05` 语义记忆是抽象的事实性知识
`05:08` 情景记忆是具体事件
`05:10` 程序记忆对应agent执行的完整步骤
`05:13` 程序记忆要求逐字保留
`05:15` 因为它真正的使用场景是agent崩溃后
`05:18` 恢复执行状态
`05:19` 几个我觉得最值得学的设计
`05:21` 第一个UUID幻觉处理
`05:24` LOOM对于长UUID字符串处理能力很差
`05:27` 会幻觉
`05:28` meme zero的做法是
`05:30` 把LLM看到了g e id
`05:32` 临时映射成012这样的简单整数
`05:35` LLM只需要决定对第三条记忆执行update
`05:39` 然后系统再把整数映射回真实u u id
`05:42` 第二个双存储并行向量存储
`05:45` 负责语义相似
`05:46` 搜索图存储
`05:48` 负责关系推理
`05:49` 两条路径
`05:50` 用threat to executor并行跑
`05:52` 结果合并返回
`05:53` 第三个双prompt策略
`05:55` mam zero有两个记忆抽取
`05:57` Prompt
`05:58` User memory extraction
`06:00` Prompt
`06:00` 只看用户消息
`06:01` Agent memory extraction
`06:03` Prompt
`06:04` 只看助手消息
`06:05` 这是一个很聪明的职责
`06:07` 分离防止AI助手的自我表达
`06:09` 污染用户记忆
`06:10` 同时也允许AI助手自己积累
`06:13` 我是一个什么样的AI的自我认知
`06:15` 第四个多层级作用域隔离
`06:18` 用户隔离
`06:19` agent隔离运行时隔离三层隔离
`06:22` 直接支持多用户多agent会话隔离的复杂场景
`06:27` mam zero很强
`06:28` 但他有一个很真实的成本瓶颈
`06:31` 每次艾特调用完整模式下
`06:33` 会调用两到五次LLM更麻烦的是
`06:36` 随着用户历史记忆的增多
`06:38` token数会随记忆规模线性增长
`06:40` 这就意味着main zero不适合高频实时写入的场景
`06:44` 它更适合用户维度
`06:46` 对话力度
`06:47` 中低频写入
`06:48` 比如AI对话助手
`06:50` 客服系统
`06:51` 医疗健康追踪这一类
`06:52` 所以说mam zero真正强的地方不是某一项黑科技
`06:56` 而是工程化的全家桶
`06:58` 第三个我想重点讲的是letter
`07:01` 如果说mam zero是把记忆层做成了成熟的中间件
`07:05` 那LETTA是在做一件更酷的事情
`07:08` 把操作系统的虚拟内存思想
`07:10` 完整地搬进agent a架构里
`07:13` 很多同学应该记得2023年10月那篇论文
`07:16` mam g p t2024年9月正式更名为LETTA
`07:20` letter的核心是一个三层记忆架构
`07:22` CORE memory核心内存直接嵌在系统
`07:25` Promptly
`07:26` 每次推理LLM都看得到
`07:28` 就像CPU能直接访问的r m archival memory
`07:32` 归档内存容量无限走向量检索
`07:36` 就像磁盘recall memory召回内存
`07:39` 存放所有历史对话消息
`07:41` 就像操作系统的日志系统
`07:43` CORE memory的每个block是一个三元组
`07:46` label是路径命名空间
`07:48` description告诉agent这个block是干什么的
`07:52` value是实际内容
`07:54` limit是字符上限
`07:55` 这个limit默认10万字符
`07:57` 它是一种强制的信息压缩约束
`08:00` 逼迫agent主动做信息蒸馏
`08:03` 当上下文窗口快满了
`08:05` 触发SUMMARIZER
`08:06` 默认驱逐30%的消息
`08:08` 被驱逐的消息写进recall memory
`08:11` 仍然可以通过conversation search再被检索出来
`08:14` 被驱逐的消息不是真的消失了
`08:16` 他们只是从in context移到了out of context
`08:20` 这才是真正意义上的虚拟内存无损分层
`08:24` LETTA有一个在同类项目里完全独树一帜的设计
`08:27` Gate enabled block manager
`08:29` 他把真正的gate引入了agent
`08:31` 记忆管理
`08:32` 双存储
`08:33` git是真实来源
`08:34` pg数据库是快速读缓存
`08:37` 每一次记忆变更都会产生一次git commit
`08:40` 携带agent的id时间戳变更原因
`08:43` 这样的好处非常多
`08:44` 不可变性内容寻址存储
`08:47` 防止静默
`08:48` 损坏完整历史
`08:49` 每一次记忆修改都能回溯并发安全
`08:52` 多个sleep time agent可以用githwork tree隔离修改
`08:56` 可审计
`08:56` 每个commit就是一条审计记录
`08:59` 再配合mam f s记忆
`09:01` 会被组织成一颗真正的目录树
`09:03` 一个目录放agent的人格设定
`09:05` 一个放用户偏好和事实知识
`09:07` 一个放学到的技能和经验
`09:09` 每一份记忆都是一个独立的markdown文件
`09:12` 另一个很妙的设计是sleep time agent
`09:15` 在用户不交互的时候
`09:17` agent也在持续自我改进
`09:19` 主agent只负责推理和回复
`09:21` 低延迟每五步触发一次sleep time agent
`09:27` memory block3个好处
`09:29` 主路径低延迟
`09:30` 后台可以用更大token预算做深度反思
`09:33` 充分利用空闲时间
`09:35` 莱塔是目前开源里记忆自制能力最强的系统
`09:39` 但代价也很明显
`09:40` 认知成本高
`09:41` 数据库强依赖80多个依赖包总结
`09:44` 如果mam zero是中间件
`09:46` LETTA就是系统第四个项目
`09:48` 来自阿里巴巴的agent scope团队叫REMY
`09:52` Remy
`09:52` 最核心的理念文件及记忆
`09:55` 传统做法
`09:56` 记忆全存数据库里
`09:57` 你想知道AI记住了你什么
`09:59` 必须通过API去查
`10:01` 整个记忆是个黑盒
`10:03` 瑞敏反过来记忆
`10:04` 直接存成markdown文件
`10:06` 你打开就能看见
`10:07` 可以直接编辑
`10:08` 可以git版本控制
`10:10` 这把记忆的控制权和透明度还给了用户
`10:13` REMI内部有两套系统
`10:15` remy light是文件记忆
`10:16` 用于短期工作记忆
`10:18` REMI本体是向量记忆
`10:20` 用于长期语义记忆
`10:21` 两套系统的时间维度是错开的
`10:23` 第一个技术亮点
`10:24` delta file washer增量监控
`10:27` 当记忆文件累积到几10KB后
`10:29` 每次变更都重新处理整个文件
`10:31` 是对embedding API的巨大浪费
`10:34` REME先检测文件是不是纯追加模式
`10:36` 如果是就只处理新增部分
`10:39` 节省92%的API的调用
`10:41` 第二个亮点
`10:42` when to use和content分离
`10:44` 它把向量嵌入建在when to use上
`10:47` 而不是content上
`10:49` 因为用户查询和wto use天然语义接近
`10:52` 大幅提升召回率
`10:54` 第三个AI自主记忆管理
`10:56` 把文件操作工具给AI
`10:59` 让AI自己决定如何组织记忆
`11:01` REI的性能数据千问38B加REMI后
`11:05` 综合得分超过了没有记忆的千问34B
`11:08` 好的记忆系统可以让小模型打过大模型
`11:11` REI是目前开源里最对人友好的记忆系统
`11:15` 最后一个要讲的是
`11:16` 整个上集里范式最激进的项目叫MAMU
`11:20` 他的slogan
`11:21` 24小时常驻主动出击的agent记忆系统
`11:24` 前面四个项目的记忆系统有一个共同点
`11:27` 本质上都是被动的
`11:28` 用户说一句话
`11:29` 系统才去add1条记忆
`11:31` 用户问一个问题
`11:33` 系统才去设置一次
`11:34` MIMIU想做的是另一个方向
`11:37` 让记忆系统不再等自己主动跑
`11:39` 用户没说话的时候
`11:41` 他在看
`11:42` 甚至在用户下一句话还没发出来之前
`11:44` 他已经预测到你大概会问什么
`11:46` 然后把相关上下文提前加载好了
`11:49` MEINU实现主动性的方式是双agent架构
`11:52` Main agent
`11:53` 干老本行
`11:54` 听用户说话
`11:55` 调工具生成回复
`11:57` mi muu bot只负责记忆这件事
`12:00` 持续盯着每一次交互
`12:01` 后台提取整理分类代码实现其实很实在
`12:06` mamu bot就是用Python的essi o dot create task
`12:10` 起了一个异步后台任务
`12:11` 靠共享conversation
`12:13` messages列表通信
`12:14` 这个设计的精妙之处在于简单到不像创新
`12:18` memo的文件系统是一个概念领域
`12:21` 底层是数据库文件夹对应category文件
`12:24` 对应memory item
`12:25` 符号链接对应交叉引用
`12:28` 挂载点对应resource和REI的用法完全不一样
`12:31` REMY是给人看的透明文本
`12:34` manual是bot自己维护的结构化目录数
`12:37` 一个偏用户主权
`12:38` 一个偏bot自制
`12:40` Memu
`12:41` 在V1.4版本引入了一个我很喜欢的机制
`12:44` 叫silence ence our memory
`12:46` 显著性感知记忆
`12:48` 具体做法是每条memory item都带一个reinforcement account
`12:52` 计数器
`12:53` 每当被检索一次计数就加一下次排序时
`12:57` 根据强化次数加权
`12:59` 越常用的记忆越容易被再次召回
`13:01` 这在模拟人类记忆的一个本质特性
`13:04` 你越经常想到的事情就越容易想起
`13:07` 我愿意把它叫做给记忆加了一层肌肉记忆
`13:10` MMU在OEMO基准上
`13:12` 跨所有推理任务的平均准确率是9%
`13:16` 12.09
`13:16` 这正是mam zero当年用来打OpenAI memory的同一个基准
`13:21` 两个框架站在同一把尺子前面
`13:23` 比MAMU给出了非常有竞争力的数字
`13:26` MEMU最适合需要长期陪伴长期学习的场景
`13:30` 个人AI助手
`13:31` 企业级客服
`13:33` Dave ops agent
`13:34` 研究型助手
`13:35` 前面四个项目
`13:37` text to ma在做记忆的语言
`13:39` mam zero在做记忆的中间件
`13:41` LETTA在做管理记忆的agent
`13:43` 瑞my在做人能看见的记忆
`13:46` 它们的共同点是记忆始终是一个被使用的对象
`13:49` Meme you
`13:50` 把这个关系反过来记忆自己成为了一个agent
`13:54` 如果让我给上级挑一句
`13:55` 最想留给观众思考的话
`13:57` 就是这一句
`13:58` 从agent有记忆到记忆本身是一个agent
`14:01` 好今天这集就先聊到这里
`14:03` 我们回头捋一下这五个项目的位置
`14:06` text to ma是最底层的记忆操作语言
`14:09` 12个操作加五元
`14:10` JASON契约
`14:12` mam zero是当下最流行的记忆中间件
`14:15` 三种技艺
`14:16` 五大工厂双存储并行
`14:18` LETTA是记忆自制做的最彻底的三层记忆
`14:22` get版本化
`14:23` Sleep time agent
`14:24` REMY走的是完全相反的哲学文件及记忆
`14:28` 把控制权交还给用户
`14:30` MAMU把记忆本身变成一个247
`14:33` 主动运行的boat
`14:34` 想快速给产品加记忆层
`14:37` 看mam zero想做真正有状态的agent
`14:39` 看LETTA想要记忆能主动预测用户
`14:42` 看MAMU想要记忆透明
`14:44` 可见看read me做研究
`14:47` 定义自己的记忆操作语言
`14:48` 看text to ma
`14:50` 下一集我们会聊更前沿的五个项目
`14:52` M m o s open viacking
`14:54` Hindsight
`14:55` Second me
`14:56` Meta ma
`14:57` 如果今天这集让你有收获
`14:58` 请一键三连支持一下下集的五个项目
`15:01` 一个比一个硬核
`15:02` 我们下期见