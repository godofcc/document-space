---
title: "AI Agent 推理模型怎么炼成"
url: "https://www.bilibili.com/video/BV1gXiCBYEim/?spm_id_from=333.1387.favlist.content.click&vd_source=bb9b0088c91e32867b62d41f2026ab57"
bvid: "BV1gXiCBYEim"
cid: "35240675370"
author: "s09g"
upload_date: "2026-01-07"
subtitle_lang: "中文"
created: "2026-05-27"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=115852766610671&bvid=BV1gXiCBYEim&cid=35240675370&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 字幕

`00:00` 今天我们说模型的推理
`00:02` 推理模型的本质其实是靠模型自己去构建一个 COT
`00:06` 也就是常说的思维链
`00:08` 对于现在比较流行的 GenAI 系统
`00:10` 你最好知道 LLM 是怎么工作的
`00:13` 现有的数据系统在里面发挥了哪些功能
`00:15` 这份内容我打算放到 system design 的基础课程里面
`00:19` 链接我放在这里
`00:20` 预训练的时候我们使用大量
`00:22` 没有标注过的文本去做自监督学习
`00:25` 然后让模型学会去预测下一个 token 的概率
`00:29` 这样你训练好的模型可以学到通用的知识和语言的规律
`00:32` 这种模型就是我们说的 base model
`00:35` 但是 base model 有一个问题
`00:36` 它不能很好地去遵循用户的指令
`00:39` 比如
`00:39` 你输入这么一个开头
`00:41` 这是我模仿 GRE 的作文题目给的一个开头
`00:44` 意思是
`00:45` 如果你上大学的时候有这么一门人比较多的基础课程
`00:49` 那么学校是不是应该去默认提供讲座录像
`00:53` 还是说应该由讲师自己去决定是不是进行录制
`00:57` 然后请根据这个主题写一篇你的回应
`00:59` GRE 是美国的研究生入学考试
`01:01` 这里主要是做一个分析性的写作
`01:04` 会去考察你的批判性思维和分析写作能力
`01:07` 一般题目中会给一个比较有争议的观点
`01:09` 然后要求你发表自己的看法
`01:12` 你要做的事情就是去选择一个立场
`01:14` 然后用一些逻辑和例子来支撑你的观点
`01:16` 如果你把这个作为一个 prompt 丢给这个 base model 的话
`01:20` base model 会给你一个什么样的回应
`01:23` Base model 的主要目标其实是 predict next token
`01:26` 它不保证把用户的输入当成一个指令
`01:29` 所以在这种情况下
`01:30` 很有可能他就是把它当成一段文本的开头
`01:32` 然后去继续往下去写
`01:34` 这里 base model 很有可能他就会说
`01:36` 你有 10 分钟的时间
`01:38` 你需要去写 100 到 150 字左右的论述
`01:41` 因为他看到语料是这样
`01:42` 他觉得这是一个题目的要求
`01:45` 所以他开始去续写这个题目的要求
`01:47` 这其实也很正常
`01:48` 因为 base model 在训练的时候
`01:50` 他的训练样本就是这么构造的
`01:51` 比如说他拿出这一段文本去进行训练
`01:54` 这里把这一段文本稍微移动那么一格
`01:56` 然后做一个 causal mask 因果遮罩
`01:59` 把后半截遮住
`02:00` 只让模型看到前面这部分的输入的这些 token
`02:04` 然后开始预测下一个即将出现的 token 是什么
`02:07` 所以 base model 它天然有一些局限性
`02:10` 它并没有被明确的去训练说这是用户正在下达指令
`02:13` 或者是这是需要你回答的问题
`02:16` 或者说你需要遵循某种格式或者角色
`02:19` Base model 在这里只能去 predict next token
`02:21` 为了解决这个问题
`02:22` 我们在这里会引入一个叫做 Supervised Fine-Tuning 的东西
`02:26` 就是我们常说的有监督的微调 SFT
`02:29` 通过 SFT 我们可以让模型去学会遵循用户的指令
`02:33` 我们要给模型一个文本的格式
`02:35` 让模型知道这里其实有不同的角色
`02:37` 比如说我可以添加一些角色的标签
`02:40` 标签其实有很多种标法
`02:41` 有 Markdown 风格
`02:42` 也有这里 XML 风格
`02:44` 像这里的话我给出一些 system 标签
`02:46` user 标签和 <assistant> 标签
`02:49` 这样的话
`02:50` 我们可以把输入系统的 prompt 做一下改写
`02:53` 在整个 prompt 的最开头
`02:55` 我们先给一个 system 标签
`02:56` 告诉他这是我们的系统级指令
`02:59` 比如说 you are a helpful assistant
`03:01` 然后我们再把用户说的这些话给他放进这个 user 标签里面
`03:05` 还是刚才那段内容
`03:07` 就是说在一个大学里的什么什么课程
`03:09` 然后吧啦吧啦一大段
`03:11` 而 model 的回复应该写在这个 <assistant> 标签之后
`03:14` 这时候 model 应该是学到的是
`03:16` 当用户提出一串问题之后
`03:18` 如果这个 <user> 标签结束了
`03:20` <assistant> 标签开始
`03:21` 那么这时候他回复的内容应该放在这个 <assistant> 标签中间
`03:25` 这样就能变成一个对话的效果
`03:27` 而训练的过程 SFT 和 Pre-training 阶段其实没有太大的区别
`03:31` 我们还是把文本移动一格
`03:33` 然后让系统去预测我接下来输出的是什么
`03:36` 在这里前面系统和 user prompt
`03:38` 这是模型不能控制部分
`03:40` 所以我们把它从这个 <assistant> 部分开始去算这个 loss
`03:44` 把前面的部分做一个 Zero out 去把它遮盖掉
`03:47` 这里当模型看到 <assistant> 之后
`03:49` 应该知道这是要我开始输出的地方
`03:51` 然后他开始从这里预测下一个会出现的 token
`03:54` 一直把这段文本继续写下去
`03:56` 在推理的时候也是同样
`03:58` 你给出这个模板
`04:00` 告诉他系统的 prompt 是什么
`04:01` 然后 user 的问句是什么
`04:03` 然后给出这个 <assistant> 标签
`04:05` 出现 <assistant> 之后
`04:06` 模型就会意识到
`04:06` 那它应该输出自己的观点了
`04:09` 本质上其实还是预测 next token
`04:11` 但是这样一来
`04:12` 模型开始能够遵循指令了
`04:14` 这个时候
`04:15` 如果你把这些完整的 prompt 丢给这个模型
`04:17` 那么经过 SFT 的 model 能够对用户像助手一样给出一个回复
`04:22` 比如说
`04:22` 模型可能回复 I support default recording
`04:25` 他先给出一个选项(or 立场)
`04:27` 然后再开始阐述自己的理由
`04:29` 这就是 instruction following 的典型效果
`04:31` SFT 这种方法适合一些固定任务
`04:33` 比如说你可以告诉他客服会有哪些话术
`04:36` 或者是一些固定领域的问答
`04:38` 比如说像 6 岁小孩解释一个什么什么问题
`04:42` 另外
`04:42` 他也能注入一些领域固定的知识
`04:44` 比如说你可以丢给他法律内容 医疗内容
`04:47` 或者是公司内部的知识库 写作规范之类的
`04:50` 但是 SFT 还是有不少缺点的
`04:53` 通常他依赖于高质量标注的数据
`04:56` 而这些数据主要是靠标注员去编写的
`04:59` 所以他很难规模化
`05:01` 如果遇上没有见过的任务或者是没有见过的要求
`05:04` 那么模型的可靠性立刻就变低了
`05:06` 另外一个问题是 SFT 他学到了只是模板和套话
`05:10` 但是这样并不能提升他事实性和推理性的回答
`05:13` 很可能会输出一些有害内容
`05:15` 这些都是 SFT 的局限性
`05:18` 想让模型的能力超出人类的水平
`05:20` 这个上限还是需要去依靠这个强化学习
`05:23` 这里一个解决方案是我们不依赖人类标注员写的答案
`05:26` 我们用模型自己来生成答案
`05:28` 然后让人来给这个答案从好到坏排个序
`05:32` 最后用这个排序的结果去训练一个奖励模型
`05:34` 也就是 reward model
`05:36` 有了这个奖励模型之后
`05:38` 我们可以给模型的每一条回复去打一个分
`05:41` 然后用这个分数去作为强化学习的奖励分数 去训练模型最大化这个得分
`05:46` 但是这里有一个问题
`05:47` 这个分数其实是描述的是人类的偏好
`05:51` 如果标注人喜欢某一条答案
`05:52` 那这条答案就会得到更高的分数
`05:54` 但是它并不代表这条答案就更好
`05:56` 这其实只是反映了一个人类的偏好
`06:00` 这叫做基于人类反馈的强化学习
`06:02` 经过这种强化学习的 model
`06:04` 它回复的质量会更好一点
`06:05` 比如你还是拿这条 prompt 丢给这个模型
`06:08` 你很可能发现这个模型开始输出一些更高质量的答案
`06:12` 但是这里是不是觉得有些问题 模型给出了一个更加符合人类偏好的回复
`06:17` 但是这不代表说这个回复就是正确的
`06:20` 因为很有可能这个模型只是在拍人类的马屁
`06:23` 有些回复不是说人喜欢就更好
`06:25` 比如说数学化学这些科学类的问题
`06:28` 或者是历史地理这些事实类的信息
`06:31` 他都不是依靠人类偏好来决定对错的
`06:34` 如果你还记得 23年 Google 第一次把 Bard 拿出来的时候
`06:38` 回答一个关于空间望远镜的问题
`06:40` 然后直接就是一个错误的答案
`06:42` 当天谷歌的市值就掉了 1,000 亿美元
`06:45` 我们希望模型给出更正确答案
`06:47` 而不仅仅是符合人类的偏好
`06:49` 那实现这一点要怎么去做呢
`06:51` 这其实源自于几个观察
`06:53` 第一个
`06:54` 模型的规模需要足够大
`06:56` 这个就是 scaling law
`06:57` 第二个观察是
`06:58` 如果模型愿意给出中间的步骤
`07:00` 那么其实可以显著地提高这个正确率
`07:03` 比如说
`07:03` 当时有篇著名的文章说
`07:06` 你直接在这 prompt 当中加入这么一句 let's think step by step
`07:10` 让我们一步步想这个问题
`07:12` 这样一来
`07:13` 模型的效果会更好
`07:14` 这个就是我们常说的思维链
`07:16` 也叫 COT
`07:17` 第三个观察是像模型它想的越久
`07:20` 那么它的表现就越好
`07:22` 这个叫做 test time compute
`07:24` 也就是测试时计算
`07:26` 这个 test time 是相对于 training time
`07:29` 因为模型提升正确率的能力不是在训练时候得到的
`07:32` 而是在推理时候提升的
`07:34` 有了这 3 点
`07:35` 其实我们就可以做一个推理模型了
`07:37` 如果你还记得我们一开始说的
`07:39` 推理模型的本质是让模型自己去构建这个 chain of thought
`07:42` 并且将中间推理的步骤展示出来
`07:45` 虽然你也可以手动去编写这个 chain of thought
`07:48` 但是问题是我们怎么才能对每一个问题都写出一个完整的思维链
`07:53` 这里最关键的一步就是让模型在输出答案之前
`07:56` 尽可能将这个大任务分解成一些小的步骤
`07:59` 然后逐步去思考
`08:00` 尽量的去思考长的时间
`08:02` 比如说你可以问模型我有 10 个苹果
`08:05` 然后分出了两个
`08:06` 然后又买了 5 个
`08:07` 又吃掉一个
`08:08` 最后会有多少个苹果
`08:09` 对非推理模型的话
`08:10` 他可能直接就给你一个数字
`08:12` 然后这个数字大概率是错的
`08:14` 但是经过思考的模型
`08:15` 他会知道
`08:16` 我将它一步步拆解出来
`08:17` 我先进行一步 think
`08:19` 比如说
`08:20` 你先有 10 个苹果
`08:21` 然后你分出一两个
`08:23` 这个时候你有 8 个
`08:25` 接下来你有了 8 个苹果
`08:26` 之后又去买了 5 个
`08:27` 那么你现在应该有 13 个苹果
`08:29` 最后你又吃掉了一个
`08:31` 那么你现在应该是 12 个苹果
`08:33` 最后他应该说出这个答案
`08:35` 说答案是 12
`08:36` 这样一来
`08:37` 我们就有一个更高的正确率
`08:38` 要做到这种级别的推理
`08:40` 实际上我们要做这么三个步骤
`08:42` 第一个
`08:42` 我们要有一些数据
`08:44` 我们可以使用一些合成数据
`08:46` 我们需要去收集一些 think 的过程
`08:48` 然后过滤筛选
`08:49` 找出正确的推理链
`08:51` 这里我们可以引入一些新的标签
`08:53` 有了这些标签
`08:54` 我们可以把数据整理成可学习的推理轨迹
`08:57` 再加上一个可验证的结果
`09:00` 在这里有很多方法
`09:01` 比如说你可以拿一个非推理的基础模型
`09:04` 然后在它的 system prompt 里直接加入这个 let's think step by step
`09:09` 然后通过这条 system
`09:10` Prompt 诱导这个模型去输出中间推理的步骤
`09:14` 甚至你可以再进一步提出一些更具体的要求
`09:17` 比如说把模型想的一些过程放在 think 标签里
`09:21` 再把它最后的答案放在 answer 标签里
`09:23` 接下来有了这些数据
`09:24` 我们就可以进行 SFT 了
`09:26` 让模型学会在推理过程中使用这个 thinking 和 answer 的标签
`09:31` 这一步叫做 Cold Start
`09:32` 然后第三步
`09:34` 我们通过强化学习的方式
`09:35` 对最终的这个 answer 字段的答案进行判断
`09:38` 去奖励正确的推理结果
`09:40` 比如说你可以一次生成多条思维链
`09:43` 然后用一个验证器(verifier)去计算
`09:45` 看哪些结论是正确的
`09:47` 给出一个分数奖励
`09:48` 然后用强化学习的算法去更新这个模型
`09:50` 最终的目的是让模型以更大的概率得出一个正确答案的轨迹分布
`09:55` 这一步叫做基于可验证奖励的强化学习
`09:58` 这里一个小的问题是
`10:00` 你怎么知道这个推理的结果是正确的
`10:02` 通常这里会存在两种方案
`10:04` 一种是我基于这个结果去进行验证
`10:07` 我直接把奖励的分数和这个结果进行挂钩
`10:10` 然后根据一些规则去得出一个数字
`10:13` 这种方法得出的奖励分数定义比较简单
`10:16` 也比较容易规模化
`10:18` 噪声也比较小
`10:19` 不需要逐步去人工生成标签
`10:21` 所以做起来会比较便宜
`10:23` 但是它的代价是这里的奖励会比较稀疏
`10:25` 完全基于最后的这个结果
`10:27` 训练上会更难
`10:29` 你需要经过更多的采样
`10:31` 再加上一些算法去做一些稳定
`10:33` 比如 deep seek
`10:34` 就把这个负的 KL 散度加到这个目标函数上
`10:37` 另一种方案是基于过程的奖励
`10:39` 这种方案的优势是它的奖励会更加稠密
`10:42` 也避免那种推理过程胡编乱造
`10:44` 但是答案凑巧碰对这种场景
`10:47` 但它的代价是你需要更高的成本
`10:49` 你需要去标注和构造整个推理的过程
`10:53` 而且它的风格会更加偏向于人类推理的风格
`10:56` 但是符合人类习惯的解法其实不一定是最优的
`10:59` 实际上从 DeepSeek 的结论来看
`11:02` 仅仅是基于结果奖励这种限制最小的方案
`11:04` 最后的效果往往是最好的
`11:06` 我们用 DeepSeek 的 R1 做一个案例
`11:08` 来看看他们是怎么训练这个推理模型的
`11:11` 首先
`11:11` 他们有一个最基础的模型叫做 V3
`11:14` 第一步他们做了一个 Cold Start 去采集了一个比较小型的推理数据集
`11:19` 这个推理数据集其实来自于他们做了一个实验
`11:23` 他们从 V3 通过仅仅使用强化学习的方式得出了一个推理模型
`11:29` 这就是 R1-Zero
`11:30` 那么 R1-Zero 有一些问题
`11:32` 比如说输出的思维链可读性比较差
`11:35` 所以他们决定还是要采取 Cold Start 的方式对 V3 的模型进行一些微调
`11:41` 他们从 R1-Zero 的输出里面整理加上人工标注
`11:44` 形成了一个小型的推理的数据集
`11:47` 然后用这个数据集对 V3 的 base model 进行了微调
`11:51` 第二步
`11:52` 他们在采用这个强化学习的方式去增强这个模型的推理能力
`11:56` 这一步就是 reasoning oriented reinforcement learning
`11:59` 这里这个模型是 R1 的前身
`12:01` 它其实已经可以做一些基础的推理了
`12:04` 比如说可以正确地使用 thinking 和 answer 标签
`12:07` 然后我们可以验证他的输出
`12:08` 去给他一些奖励
`12:10` 比如说看他给出的代码是不是能够编译
`12:13` 能不能通过单元测试
`12:15` 这里 DeepSeek 给了两种奖励的模式
`12:17` 一种是准确度的激励
`12:18` 如果他的答案是正确的
`12:20` 那么会给一些奖励的分数
`12:21` 如果答案错误
`12:22` 那么就不加分
`12:24` 另外一种是格式的奖励
`12:25` 比如说模型必须按照要求的格式作答
`12:28` 如果模型在 thinking 标签中先写出这个推理过程
`12:32` 然后在这个 answer 中再给出这个答案
`12:34` 那他就会有一个更高的分数
`12:36` 这都是一些很简单的奖励规则
`12:38` 然后再采取这个 GRPO 进行训练
`12:41` DeepSeek 团队发现通过强化学习
`12:44` 模型自己就发现只要他能够尽量去更长的思考
`12:47` 那么他得出正确答案的概率就会更高
`12:50` 最后得到的分数也就更高
`12:52` 第三步他们去做了一个拒绝采样
`12:54` 他们用这个经过了强化学习
`12:56` 增强了推理能力的模型去生成了一些推理的数据
`13:00` 然后再用这个 base model 去对这个数据进行一些过滤和清洗
`13:04` 比如说我判断这个推理是不是基于事实
`13:07` 最后得出了一个高质量的推理数据的样本
`13:10` 另外他们再从这个 V3 的 base model 上生成一些非推理的样本
`13:15` 比如说去做一些写作或者是问答或者是翻译的任务
`13:18` 有了这两部分数据之后
`13:20` 他们又做了一个 SFT 去微调原本的这个 V3 的 model
`13:24` 这样一来就得到一个推理模型
`13:26` 最后他们再去做人类偏好对齐
`13:28` 去减少那些有偏见或者是有害内容的输出
`13:32` 那最后得出来的这个 model 就是 DeepSeek R1 了
`13:34` 你可能已经注意到了整个 RL 阶段
`13:37` 它的训练方式和我们之前所说的 Pre training 或者是 SFT 也是不一样的
`13:41` 强化学习的一个难点其实是在这个 infrastructure 上